language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-isgadget' ]; then cd .. && eval "mv $currentfolder generator-isgadget" && cd generator-isgadget; fi

